server:
  port: 8081

spring:
  application:
    name: CMS-Project
  datasource:
    url: jdbc:mysql://localhost:3306/cms
    username: root
    password: Tiger2143@+%
  jpa:
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
